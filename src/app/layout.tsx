import type { Metadata } from 'next';
import '@/styles/global.css';
import Header from '../shared/components/layout/Header';
import ModalContainer from '@/shared/components/ui/ModalContainer';
import { MSWComponent } from '@/mocks/MSWComponent';
import { initMocks } from '@/mocks';
import Head from 'next/head';

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

if (process.env.NODE_ENV === 'development') {
  initMocks();
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ko">
      <Head>
        <link
          rel="preload"
          href="https://cdn.jsdelivr.net/gh/sun-typeface/SUIT@2/fonts/variable/woff2/SUIT-Variable.woff2"
          as="font"
          type="font/woff2"
          crossOrigin="anonymous"
        />
        <style
          dangerouslySetInnerHTML={{
            __html: `
        @font-face {
        font-family: 'SUIT';
        src:url("https://cdn.jsdelivr.net/gh/sun-typeface/SUIT@2/fonts/variable/woff2/SUIT-Variable.woff2") format("woff2-variations");
        font-weight:100 900;
        font-display:swap;
        }
        `,
          }}
        />
      </Head>
      <body>
        <MSWComponent>
          <Header />
          <ModalContainer />
          {children}
        </MSWComponent>
      </body>
    </html>
  );
}
