/** @type {import("prettier").Config} */
const config = {
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  tabWidth: 2,
  printWidth: 100,
  bracketSameLine: false,
  arrowParens: 'always',
  bracketSpacing: true,
}

export default config
